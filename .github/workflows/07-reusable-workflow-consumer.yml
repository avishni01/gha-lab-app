name: 07 Reusable Workflow Consumer

on:
  workflow_dispatch:

jobs:
  # TODO: After splitting this lab into separate repositories, replace this local
  # path with:
  # uses: OWNER/gha-lab-shared-workflows/.github/workflows/reusable-python-ci.yml@main
  call-local-python-ci:
    name: Call local reusable Python CI
    uses: ./.github/workflows/reusable-python-ci-placeholder.yml
    with:
      python-version: "3.12"
      test-command: pytest

